#set( $nameLC = ${entity.name.toLowerCase()} )
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE tiles-definitions PUBLIC
	"-//Apache Software Foundation//DTD Tiles Configuration 2.1//EN"
	"http://tiles.apache.org/dtds/tiles-config_2_1.dtd">

<tiles-definitions>
	<definition name="${nameLC}.import.list" extends="unit">
		<put-attribute name="body" value="views/${nameLC}/${nameLC}-import-list.html" />
	</definition>

	<definition name="${nameLC}.import.search" extends="ajax">
		<put-attribute name="body" value="views/${nameLC}/${nameLC}-import-search.html" />
    </definition>

	<definition name="${nameLC}.import.table" extends="ajax">
		<put-attribute name="body" value="views/${nameLC}/${nameLC}-import-table.html" />
    </definition>

</tiles-definitions>
